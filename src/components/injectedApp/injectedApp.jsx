import React from "react";
// import { useSelector, useDispatch } from "react-redux";

const InjectedApp = () => {
  // const dispatch = useDispatch();
  // const setting = useSelector((state) => state.setting);

  return (
    <>
      <div>Injected Script</div>
    </>
  );
};

export default InjectedApp;
